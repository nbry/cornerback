# cornerback-postgres-eventstore

Postgres implementation of cornerback's `EventStore`.

## Setup

1. Create a Postgres database and user for cornerback.
2. Run the SQL in `schema.sql` to create the necessary tables.
